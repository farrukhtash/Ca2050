source "https://rubygems.org"

# Built via GitHub Actions (not the default Pages builder), so we can use plugins.
gem "jekyll", "~> 4.3"
gem "jekyll-polyglot", "~> 1.8"

# Needed on Ruby 3.x
gem "webrick", "~> 1.8"
